using FindPath;
using UnityEngine;

public static class SurfaceFinder 
{
    public static Tile.Surface GetSurface(Vector3 pos, Tile tile = null)
    {
        if (tile != null)
        {
            
        }
    }
}
